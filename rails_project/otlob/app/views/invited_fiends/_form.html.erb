<%= form_for(@invited_fiend) do |f| %>
  <% if @invited_fiend.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@invited_fiend.errors.count, "error") %> prohibited this invited_fiend from being saved:</h2>

      <ul>
      <% @invited_fiend.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :status %><br>
    <%= f.text_field :status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
