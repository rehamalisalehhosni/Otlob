<p id="notice"><%= notice %></p>



<div class="col-sm-12">
  <h3>Friends</h3>


    <% @friends.each do |friend| %>

        <div class="col-sm-3">
          <img class="img-responsive img-thumbnail" width="150" height="150" src='/uploads/user/image/<%= friend.friend_id %>/<%= friend.image %>' ><br><h5 class="pull-left" style="margin-left: 10px"></h5><%= friend.name   %></h5><p>

              <%= link_to 'unfriend', friend, method: :delete, data: { confirm: 'Are you sure?' } %>
            </p>
        </div>
    <% end %>

</div>

