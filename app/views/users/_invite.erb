<%= form_for(@user=User.new) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Invited_Friends %><br>
  <%= f.autocomplete_field :name, autocomplete_user_name_users_path,'data-delimiter' => ',', :multiple => true , 'data-auto-focus' => true %><br>
  </div>

<% end %>
